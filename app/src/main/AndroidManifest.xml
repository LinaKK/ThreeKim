<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studyforce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".whole_class_list"></activity>
        <activity android:name=".Sign" />
        <activity android:name=".Login" />
        <activity android:name=".lock_setting" />
        <activity android:name=".my_class_list" />
        <activity android:name=".personal_schedule" />
        <activity android:name=".personal_info" />
        <activity android:name=".personal_page" />
        <activity android:name=".CreateByT" />
        <activity android:name=".CreateByStu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>